[project]
name = "yyy-monkey-local-service"
version = "0.1.0"
description = "Local control plane for yyy_monkey (FastAPI + SQLite)."
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.34.0",
  "pydantic==2.10.3",
]

[tool.uv]
dev-dependencies = []


